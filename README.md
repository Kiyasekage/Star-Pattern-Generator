# Star-Pattern-Generator
A Python program that generates a star (*) pattern that first increases to a user-defined maximum length and then decreases back to one. The project demonstrates nested loops, string multiplication, and pattern generation techniques commonly used in introductory programming exercises.
